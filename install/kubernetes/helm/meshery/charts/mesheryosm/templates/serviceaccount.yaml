{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "mesheryosm.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "mesheryosm.labels" . | nindent 4 }}
{{- end -}}
