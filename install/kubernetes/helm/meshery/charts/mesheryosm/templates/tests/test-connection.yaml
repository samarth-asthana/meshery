{{- if .Values.testCase.enabled -}}
apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mesheryosm.fullname" . }}-test-connection"
  labels:
{{ include "mesheryosm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "mesheryosm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
{{- end -}}